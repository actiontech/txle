全局事务
 1 后端提供全局事务列表数据，默认按结束时间降序展示，且仅默认展示全局事务，不主动展示子事务；
 2 后端提供读取表头api列名称，每个页面涉及有哪些交互api，api有哪些；
 3. vue -txle-ui 已经确定现有UI风格；已确定demo父子节点效果；已确定搜索由后端提供支持，所有筛选排序都后端来做；
 4. 估计列表表头名：全局事务标识，服务名称，服务代号，服务类别，超时期限，重试次数，状态，开始时间，结束时间；后端提供；
 5. UI按钮：显示子事务 ，隐藏子事务，暂停全局事务，继续全局事务，终止全局事务;
 6. 估计涉及API：暂停全局事务api，继续全局事务api，终止全局事务api，列表默认展示搜索|排序api；列表表头api，显示子事务 api，子事务无操作；后端提供这些url；
 7. api 是get还是post 请求方式由辛斌燕决定；
 8. API排序列：除全局事务标识外的字段排序筛选全部支持；
 9. 估计状态选项：对应名称=成功、异常、暂停、继续、运行中 | 对应数值=？，后端提供这些key value；
 10. 暂停全局事务、继续全局事务、终止全局事务，若已存在全局事务的结束时间，则自动过滤掉，如果都存在全局事务结束时间，则无需触发请求后台。（需要前端做什么？感觉是后端做好就行）
 11. 计算属性或同等变量：在显示子事务或隐藏子事务的操作数据过程中使用；
 12. 右边三个选项按钮在没有行选中时，需要提示确认.即暂停全局事务，继续全局事务，终止全局事务 confirm 不选择某一行或几行针对所有行；
 13. 斑马线隔行换色在前端开发中注意美观；
 14. 单页面图标统一，鼠标移动到图标提示文字含义或者另起一行罗列各个图标含义；
 15. 前两个按钮显示子事务 ，隐藏子事务，看实际情况disable  处理或者点击后提示未选择行  ；disable  处理：默认禁用，显示行选中去除禁用关系 ；
配置中心
 1. 估计列表表头名： 主键,服务名称,服务代号,服务类别,类型,状态,能力,值,备注,更新时间；后端提供这些；
 2. UI按钮：新增,保存,删除
 3. 估计涉及API：新增api,保存api,删除api,列表默认展示api,列表搜索api,列表排序api,三级联动api,三级联动自定义api;后端提供；
 4. 服务类别选项：对应名称=? |  对应数值=？后端提供；
 5.  类型选项：对应名称=全局事务、手动补偿、自动补偿、业务消息上报、事务监控、告警、定时器、全局事务容错、手动补偿容错、自动补偿容错、异常快照上报、SQL监控|  对应数值=？后端提供；
 6. 状态选项：对应名称=正常、历史、废弃 |  对应数值=？后端提供；
 7. 能力选项：对应名称=有，无 |  对应数值=？后端提供；
 8. 值选项：对应名称=启用，禁用，【自定义】 |  对应数值=？后端提供；
 9. 服务名称、服务代号、服务类别：支持三级联动、支持自定义。 这是在添加的界面呈现。三个都是下拉框，其中服务名称选中后影响服务代号、服务类别，服务代号选中后影响服务类别，总之三级联动在弹层里面显示;
 10.新增编辑点击出弹层；
 11.新增/编辑/删除  提示风格和前面全局事务一样；编辑保存删除需二次confirm，保存时提示风险，不自动更新相关子事务状态，总之操作流程风格统一并且流畅;
 12.新增/编辑 涉及字段由后端提供必填项、可选项、验证长度 |类型；
 13.后端提供所有下拉选项提供key value；
 14.表格展示数据格式化输出，避免表格被数据的特殊字符破坏；
 15.删除按钮需要确认；
 16.全部字段支持排序；
差错处理
 1 后端提供表头api，大概的样子如下：
    全局事务标识,子事务标识,服务名称,业务信息,类型,状态,上报时间,完成时间，处理人，备注
    api预计提供列名的中文名称和数据字段相应的字段名以及每个字段的字长。
 2 UI按钮：纯展示，无交互按钮。
 3 后端提供列表api：支持列表默认展示、搜索、全字段排序
 4 后端提供状态选项：对应名称=发送中，已发送，发送失败，(处理)成功，(处理)失败  |  对应数值=？
 5.图标处理与全局事务一样的方式；
 6.业务信息超出长度鼠标移上去出弹层滚动查看内容。

 ----------------------------------------------------
 开发注意事项：
 排序功能编写；
 表头读取api编写；
 斑马线效果编写；
 表格提示层编写。





